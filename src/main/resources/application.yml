
server:
  port: 8090
spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: hyeonjae1023111@gmail.com
    password: knqn oqzt dyqk xcqf

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/sbb_dev
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true